declare namespace App {
  export interface PixProps {
    bank: string
    doc: string
    key: string
    name: string
  }
  export interface KeyPixProps {
    id: string
    type: string
    code: string
    created: string
  }
}
